// Placeholder seed script.
// When Prisma is wired, you can import PrismaClient and seed data here.

async function main() {
  // eslint-disable-next-line no-console
  console.log("Seed placeholder (no-op)");
}

main().catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
